#include <stdio.h>

int howManyTimesaMonth(char monthProvided, char minutes, char hours, char days, char months, char weekdays) {
	int times = 1;
	if (minutes == '*') {
		times *= 60;
	}
	if (hours == '*') {
		times *= 24;
	}
	if (days == '*') {
		times *= :
	}

}

int main(int argcount, char *argvalue[]) {
	

	exit(EXIT_SUCCESS);
}
